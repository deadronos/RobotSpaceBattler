// global.d.ts intentionally removed — debug flags and test helper globals
// are now defined and accessed via `src/utils/debugFlags.ts`.
// Keeping this file as an inert placeholder to avoid accidental re-addition.
