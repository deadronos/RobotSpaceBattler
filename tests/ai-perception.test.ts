import { beforeEach, describe, it } from "vitest";
import { resetWorld } from "../src/ecs/miniplexStore";

describe("AI Perception Helpers", () => {
  beforeEach(() => {
    resetWorld();
  });

  // DELETED: ai-perception.test.ts removed per user confirmation. Placeholder retained.
});
